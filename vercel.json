{
  "buildCommand": "prisma generate && prisma db deploy && tsx prisma/seed.ts && yarn build",
  "installCommand": "yarn install",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 10
    }
  },
  "env": {
    "PRISMA_GENERATE_SKIP_AUTOINSTALL": "true"
  }
}
